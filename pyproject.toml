[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "swing-trader"
version = "0.1.0"
description = "Rule-based swing trading scanner and backtesting toolkit for equities and crypto."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26",
  "pandas>=2.2",
  "PyYAML>=6.0",
  "yfinance>=0.2.54",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
swing-trader = "swing_trader.cli:main"
swing-backtest = "swing_trader.backtest_cli:main"
swing-experiment = "swing_trader.experiment_cli:main"
swing-cost-sensitivity = "swing_trader.cost_sensitivity_cli:main"
swing-temporal-stability = "swing_trader.temporal_stability_cli:main"
swing-reference-comparison = "swing_trader.reference_comparison_cli:main"
swing-parameter-neighborhood = "swing_trader.parameter_neighborhood_cli:main"
swing-holdout-record = "swing_trader.prospective_recorder_cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
