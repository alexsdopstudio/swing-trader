experiment:
  id: EXP-0002
  name: execution-cost-sensitivity
  research_stage: sensitivity
  base_experiment_config: experiments/EXP-0001-baseline/config.yaml
  reference_results: experiments/EXP-0001-baseline/results.json

scenarios:
  low: 0.5
  baseline: 1.0
  high: 2.0
  stress: 4.0

research_questions:
  primary_scenario: high
  stress_scenario: stress
